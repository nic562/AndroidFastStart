<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.nic562.androidFastStartDemo">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme">

        <activity android:name=".ActivityMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ActivityCropImage" />

        <activity android:name=".ActivityCropImage2" />

        <activity android:name=".ActivitySMS" />

        <activity android:name=".ActivityCard" />

        <activity android:name=".ActivityCardRaw" />

        <activity android:name=".ActivityTree" />

        <activity android:name=".ActivityPage" />

        <activity
            android:name=".ActivityColl"
            android:theme="@style/MaterialTheme.NoActionBar" />
    </application>
</manifest>
